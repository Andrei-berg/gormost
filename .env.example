# Supabase 2026 keys — Project Settings -> API Keys
#   Publishable key  sb_publishable_...  (replaces anon)
#   Secret key       sb_secret_...       (replaces service_role)
# The code also reads the legacy names as a fallback (see src/lib/supabase.ts).

NEXT_PUBLIC_SUPABASE_URL=https://wwwtsvboqffzbnliuiun.supabase.co
SUPABASE_PUBLISHABLE_KEY=sb_publishable_xxx
SUPABASE_SECRET_KEY=sb_secret_xxx

# HMAC secret for the session cookie:  openssl rand -hex 32
SESSION_SECRET=
