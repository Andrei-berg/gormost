---
phase: 04-staff-management
plan: 05
type: execute
wave: 4
depends_on:
  - 04-04
files_modified:
  - src/components/hr/EmployeeCard.tsx
autonomous: true
requirements:
  - HR-16

must_haves:
  truths:
    - "EmployeeCard shows 10 clickable status buttons in two rows when canEdit is true"
    - "Row 1 (existing): На работе, Отгул, Больничный, Отпуск"
    - "Row 2 (new): Командировка, Учебный отпуск, Декрет, Мобилизован, СВО, Вернулся с СВО"
    - "Active status is highlighted using its EMPLOYEE_STATUS_CONFIG color"
    - "Clicking any new status button works identically to existing buttons — optimistic update + INSERT"
    - "Reason input appears for all non-Na_rabote statuses including the 6 new ones"
    - "Uvolen is NOT shown as a clickable status button"
    - "npm run build passes"
  artifacts:
    - path: "src/components/hr/EmployeeCard.tsx"
      provides: "Extended status buttons — 10 total in 2 rows"
      contains: "Komandirovka"
  key_links:
    - from: "EmployeeCard CLICKABLE_STATUSES"
      to: "EMPLOYEE_STATUS_CONFIG"
      via: "forEach rendering buttons"
      pattern: "CLICKABLE_STATUSES"
---

<objective>
Add the 6 new status buttons to EmployeeCard as a second row.

Purpose: HR-16 requires Командировка, Учебный отпуск, Декрет, Мобилизован, СВО, Вернулся с СВО to be clickable statuses in the HR panel. The types and config were added in plan 04-03. This plan makes them visible in the UI.

Output: Single file change to EmployeeCard.tsx. All logic (optimistic update, reason input, INSERT) already exists — only the CLICKABLE_STATUSES list and layout need updating.
</objective>

<execution_context>
@/home/user/.claude/get-shit-done/workflows/execute-plan.md
@/home/user/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@src/components/hr/EmployeeCard.tsx

<interfaces>
<!-- Current CLICKABLE_STATUSES constant (to extend): -->
```typescript
const CLICKABLE_STATUSES: EmployeeStatusType[] = ['Na_rabote', 'Otgul', 'Bolnichniy', 'Otpusk']
```

<!-- EMPLOYEE_STATUS_CONFIG now has 11 entries (added in plan 04-03).
     New entries: Komandirovka, Uchebniy_otpusk, Dekret, Mobilizovan, SVO, Troydoustroyen_s_SVO
     Each has: label (Russian), color (hex), bg (Tailwind classes) -->

<!-- Existing button render pattern (from EmployeeCard.tsx): -->
```tsx
{CLICKABLE_STATUSES.map(status => (
  <button
    key={status}
    onClick={() => handleStatusClick(status)}
    disabled={saving}
    className={`... ${localStatus === status ? cfg.bg : 'bg-white/5'} ...`}
  >
    {EMPLOYEE_STATUS_CONFIG[status].label}
  </button>
))}
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend EmployeeCard — two-row status button layout with 6 new statuses</name>
  <files>src/components/hr/EmployeeCard.tsx</files>
  <action>
Edit `src/components/hr/EmployeeCard.tsx`. Make minimal targeted changes:

1. **Replace CLICKABLE_STATUSES constant** — split into two rows for visual grouping:
```typescript
// Row 1: daily/operational statuses (unchanged from Phase 03)
const DAILY_STATUSES: EmployeeStatusType[] = ['Na_rabote', 'Otgul', 'Bolnichniy', 'Otpusk']

// Row 2: extended statuses (Phase 04) — Uvolen NOT included (it's a lifecycle event)
const EXTENDED_STATUSES: EmployeeStatusType[] = [
  'Komandirovka', 'Uchebniy_otpusk', 'Dekret', 'Mobilizovan', 'SVO', 'Troydoustroyen_s_SVO'
]
```

2. **Update the status buttons render section** — replace the single row with two rows:
```tsx
{canEdit && (
  <div className="space-y-1.5 mt-2">
    {/* Row 1 — daily statuses */}
    <div className="flex flex-wrap gap-1">
      {DAILY_STATUSES.map(status => {
        const statusCfg = EMPLOYEE_STATUS_CONFIG[status]
        return (
          <button
            key={status}
            onClick={() => handleStatusClick(status)}
            disabled={saving}
            className={`px-2 py-0.5 rounded text-xs font-medium border transition-colors ${
              localStatus === status
                ? statusCfg.bg
                : 'bg-white/5 border-white/10 text-white/50 hover:bg-white/10'
            }`}
          >
            {statusCfg.label}
          </button>
        )
      })}
    </div>
    {/* Row 2 — extended statuses (Phase 04) */}
    <div className="flex flex-wrap gap-1">
      {EXTENDED_STATUSES.map(status => {
        const statusCfg = EMPLOYEE_STATUS_CONFIG[status]
        return (
          <button
            key={status}
            onClick={() => handleStatusClick(status)}
            disabled={saving}
            className={`px-2 py-0.5 rounded text-xs font-medium border transition-colors ${
              localStatus === status
                ? statusCfg.bg
                : 'bg-white/5 border-white/10 text-white/50 hover:bg-white/10'
            }`}
          >
            {statusCfg.label}
          </button>
        )
      })}
    </div>
  </div>
)}
```

3. **Keep all existing logic unchanged:** handleStatusClick, handleReasonConfirm, optimistic update, reason input section, error display, StatusHistory accordion. These all work for any EmployeeStatusType — no changes needed.

4. The `showReason` logic uses `newStatus !== 'Na_rabote'` which already covers the new statuses correctly — no change needed.

5. Remove the old `CLICKABLE_STATUSES` constant since it is replaced by `DAILY_STATUSES` and `EXTENDED_STATUSES`.

After editing, run `npm run build` to verify no TypeScript errors.
  </action>
  <verify>
    <automated>cd /home/user/Projects/gormost && npm run build 2>&1 | tail -5</automated>
  </verify>
  <done>
    - DAILY_STATUSES and EXTENDED_STATUSES constants replace old CLICKABLE_STATUSES
    - Status buttons render in two rows under canEdit condition
    - All 10 statuses clickable (4 + 6)
    - Uvolen not shown as a button
    - All existing behavior (optimistic update, reason input, StatusHistory) unchanged
    - npm run build passes with no errors
  </done>
</task>

</tasks>

<verification>
- src/components/hr/EmployeeCard.tsx has DAILY_STATUSES and EXTENDED_STATUSES
- Two-row button layout renders under canEdit
- npm run build passes
- No regression in existing status button behavior
</verification>

<success_criteria>
- EmployeeCard shows 10 status buttons in two rows
- New statuses Komandirovka, Uchebniy_otpusk, Dekret, Mobilizovan, SVO, Troydoustroyen_s_SVO are clickable
- Active status highlighted correctly
- npm run build passes
</success_criteria>

<output>
After completion, create `.planning/phases/04-staff-management/04-05-SUMMARY.md`
</output>
