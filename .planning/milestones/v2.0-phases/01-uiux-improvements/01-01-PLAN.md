---
phase: 01-uiux-improvements
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/components/EmptyState.tsx
  - src/components/KanbanBoard.tsx
  - src/components/dispatcher/TableView.tsx
  - src/app/head/page.tsx
  - src/app/boss/page.tsx
  - src/app/zamporab/page.tsx
autonomous: true
requirements:
  - REQ-132

must_haves:
  truths:
    - "Empty kanban columns show an icon + message instead of bare dimmed text"
    - "Empty dispatcher table shows an icon + message instead of bare dimmed text"
    - "Empty request list in head/boss/zamporab panels shows an icon + message"
    - "All empty state appearances are visually consistent (same icon, same style)"
  artifacts:
    - path: "src/components/EmptyState.tsx"
      provides: "Shared empty state component"
      exports: ["default EmptyState"]
    - path: "src/components/KanbanBoard.tsx"
      provides: "Kanban with EmptyState replacing bare text"
      contains: "EmptyState"
    - path: "src/components/dispatcher/TableView.tsx"
      provides: "Table with EmptyState replacing bare text"
      contains: "EmptyState"
  key_links:
    - from: "src/components/KanbanBoard.tsx"
      to: "src/components/EmptyState.tsx"
      via: "import EmptyState from './EmptyState'"
      pattern: "import EmptyState"
    - from: "src/components/dispatcher/TableView.tsx"
      to: "src/components/EmptyState.tsx"
      via: "import EmptyState from '../EmptyState'"
      pattern: "import EmptyState"
---

<objective>
Create the shared EmptyState component and replace all 5 bare empty state text strings in the codebase with it.

Purpose: Consistent, polished UI when lists are empty — icon + message instead of bare dimmed text.
Output: src/components/EmptyState.tsx + 5 files updated to import and use it.
</objective>

<execution_context>
@/Users/andrei/.claude/get-shit-done/workflows/execute-plan.md
@/Users/andrei/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md

<interfaces>
<!-- Key types and patterns the executor needs. Extracted from codebase. -->

From src/components/KanbanBoard.tsx line 69-71 (CURRENT — replace this):
```typescript
{cards.length === 0 && (
  <div className="text-xs text-white/20 text-center py-8">Нет заявок</div>
)}
```

From src/components/dispatcher/TableView.tsx lines 47-49 (CURRENT — replace this):
```typescript
{requests.length === 0 && (
  <tr><td colSpan={6} className="px-4 py-12 text-center text-white/20">Нет заявок</td></tr>
)}
```

From src/app/head/page.tsx line 163-165 (CURRENT — replace this):
```typescript
<div className="text-center text-white/20 py-20">Нет заявок по вашей службе</div>
```

From src/app/boss/page.tsx line 110 (CURRENT — replace this):
```typescript
{pendingApproval.length === 0 && <div className="text-center text-white/20 py-20">Нет заявок на утверждение</div>}
```

From src/app/zamporab/page.tsx lines 177-179 (CURRENT — replace this):
```typescript
<div className="glass rounded-xl p-6 text-center text-white/20 text-sm border border-dashed border-white/10">
  Нет заявок — план не составлен
</div>
```

Established style from project (badge pattern reference):
- Glass cards: `className="glass rounded-xl p-4"`
- Dimmed text: `text-white/40`
- Centered content: `flex flex-col items-center justify-center`
- Spacing pattern: `py-20` for page-level empty states
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Create EmptyState component</name>
  <files>src/components/EmptyState.tsx</files>
  <action>
Create a new file `src/components/EmptyState.tsx` with a shared empty state component.

Component spec:
- Named: `EmptyState`
- Props interface: `{ message?: string; icon?: string }`
- Default `message`: `'Заявок нет'`
- Default `icon`: `'📭'`
- Layout: `flex flex-col items-center justify-center py-20 text-white/40`
- Icon rendered at `text-4xl mb-3`
- Message rendered at `text-sm`
- Export: `export default function EmptyState`

Full implementation:
```typescript
interface Props {
  message?: string
  icon?: string
}

export default function EmptyState({ message = 'Заявок нет', icon = '📭' }: Props) {
  return (
    <div className="flex flex-col items-center justify-center py-20 text-white/40">
      <span className="text-4xl mb-3">{icon}</span>
      <span className="text-sm">{message}</span>
    </div>
  )
}
```

No `'use client'` directive needed — this is a pure presentational component with no hooks.
  </action>
  <verify>
    <automated>npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>File exists at src/components/EmptyState.tsx, exports default EmptyState, TypeScript check passes.</done>
</task>

<task type="auto">
  <name>Task 2: Replace all 5 bare empty state strings</name>
  <files>
    src/components/KanbanBoard.tsx,
    src/components/dispatcher/TableView.tsx,
    src/app/head/page.tsx,
    src/app/boss/page.tsx,
    src/app/zamporab/page.tsx
  </files>
  <action>
Replace bare empty state text in all 5 locations with the new EmptyState component. Exact changes:

**1. src/components/KanbanBoard.tsx**

Add import at top (after existing imports):
```typescript
import EmptyState from './EmptyState'
```

Replace line 69-71:
```typescript
// OLD:
{cards.length === 0 && (
  <div className="text-xs text-white/20 text-center py-8">Нет заявок</div>
)}
// NEW:
{cards.length === 0 && <EmptyState message="Нет заявок" />}
```

Note: KanbanBoard renders EmptyState inside a column `<div>` (not a table), so no `<tr>` wrapper needed.

**2. src/components/dispatcher/TableView.tsx**

Add import at top (no `'use client'` in this file — add import after existing imports):
```typescript
import EmptyState from '../EmptyState'
```

Replace lines 47-49:
```typescript
// OLD:
{requests.length === 0 && (
  <tr><td colSpan={6} className="px-4 py-12 text-center text-white/20">Нет заявок</td></tr>
)}
// NEW:
{requests.length === 0 && (
  <tr><td colSpan={6}><EmptyState message="Нет заявок" /></td></tr>
)}
```

IMPORTANT: Keep the `<tr><td colSpan={6}>` wrapper — EmptyState must be inside a valid table structure. Do NOT render a bare `<EmptyState>` as direct child of `<tbody>`.

**3. src/app/head/page.tsx**

Add import (after existing imports):
```typescript
import EmptyState from '@/components/EmptyState'
```

Find the empty state div (around line 163-165) and replace:
```typescript
// OLD:
<div className="text-center text-white/20 py-20">Нет заявок по вашей службе</div>
// NEW:
<EmptyState message="Нет заявок по вашей службе" />
```

**4. src/app/boss/page.tsx**

Add import (after existing imports):
```typescript
import EmptyState from '@/components/EmptyState'
```

Find the empty state div (around line 110) and replace:
```typescript
// OLD:
{pendingApproval.length === 0 && <div className="text-center text-white/20 py-20">Нет заявок на утверждение</div>}
// NEW:
{pendingApproval.length === 0 && <EmptyState message="Нет заявок на утверждение" />}
```

**5. src/app/zamporab/page.tsx**

Add import (after existing imports):
```typescript
import EmptyState from '@/components/EmptyState'
```

Find the dashed-border empty state div (around lines 177-179) and replace:
```typescript
// OLD:
<div className="glass rounded-xl p-6 text-center text-white/20 text-sm border border-dashed border-white/10">
  Нет заявок — план не составлен
</div>
// NEW:
<EmptyState message="Нет заявок — план не составлен" />
```

Note: Drop the dashed border wrapper — consistent EmptyState styling supersedes it per user decision.

After all changes, run: `npm run build`
If build fails, check TypeScript import paths (use `@/components/EmptyState` for app/ files, `./EmptyState` for components/ siblings, `../EmptyState` for components/dispatcher/ files).
  </action>
  <verify>
    <automated>npm run build 2>&1 | tail -20</automated>
  </verify>
  <done>
Build passes. All 5 files import EmptyState and use it. No bare Russian empty state strings remain in KanbanBoard.tsx, TableView.tsx, head/page.tsx, boss/page.tsx, zamporab/page.tsx.
Verify with: grep -r "Нет заявок\|Нет заявок по\|Нет заявок на\|план не составлен" src/components/KanbanBoard.tsx src/components/dispatcher/TableView.tsx src/app/head/page.tsx src/app/boss/page.tsx src/app/zamporab/page.tsx — should return no matches (strings are now inside EmptyState component itself).
  </done>
</task>

</tasks>

<verification>
Run `npm run build` — must exit 0 with no TypeScript errors.
Check that visiting the app with no requests shows the icon + message in empty panels.
</verification>

<success_criteria>
- src/components/EmptyState.tsx exists with correct interface and default props
- All 5 previously-bare empty state texts replaced with EmptyState component
- Build passes (npm run build exits 0)
- No broken HTML structure (TableView wraps EmptyState in tr/td)
</success_criteria>

<output>
After completion, create `.planning/phases/01-uiux-improvements/01-01-SUMMARY.md`
</output>
